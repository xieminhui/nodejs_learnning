const circle = require('./circle.js');


console.log(`半径为4的圆的面积是${circle.area(4)}`);

console.log('------------------');
console.log('main start');
const a = require('./a.js');
const b = require('./b.js');
console.log('在mian中，a.done = %j,b.done=%j', a.done, b.done);
console.log('------------------');

